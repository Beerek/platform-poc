server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  file: application.log
  level:
    com:
      netflix:
        eureka: DEBUG
        discovery: DEBUG